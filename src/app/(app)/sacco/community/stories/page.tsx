'use client'

import { PlannedFeatureState } from '@/components/sacco/shared'

export default function CommunityStoriesPage() {
  return (
    <PlannedFeatureState
      title="Success Stories"
      description="Member story-sharing is paused until the supporting community workflows are fully built."
      phase="Planned experience layer"
    />
  )
}
