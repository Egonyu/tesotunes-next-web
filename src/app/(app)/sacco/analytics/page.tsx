'use client'

import { PlannedFeatureState } from '@/components/sacco/shared'

export default function AnalyticsPage() {
  return (
    <PlannedFeatureState
      title="Production Analytics"
      description="Creator analytics and forecasting will return after the SACCO finance core is fully stabilized."
      phase="Phase 2 rebuild"
    />
  )
}
