'use client'

import { PlannedFeatureState } from '@/components/sacco/shared'

export default function ContributionsPage() {
  return (
    <PlannedFeatureState
      title="Contributions"
      description="Contribution tracking needs a dedicated SACCO accounting flow before it can be exposed in the member app."
      phase="Phase 2 rebuild"
    />
  )
}
