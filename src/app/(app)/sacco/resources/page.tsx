'use client'

import { PlannedFeatureState } from '@/components/sacco/shared'

export default function ResourcesPage() {
  return (
    <PlannedFeatureState
      title="Platform Resources"
      description="Resource marketplace and booking flows are planned for a later creator-finance phase and are not active yet."
      phase="Phase 2 rebuild"
    />
  )
}
